# SteamOS quirk: pkgconf's default search path doesn't reliably include
# /usr/lib/pkgconfig and /usr/share/pkgconfig in the cargo build environment.
# Setting PKG_CONFIG_PATH here ensures rdev's x11 dependency can be resolved.
[env]
PKG_CONFIG_PATH = "/usr/lib/pkgconfig:/usr/share/pkgconfig"
